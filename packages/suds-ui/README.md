# suds-ui

Streamlit UI for interacting with SUDS FastAPI.

## Requirements
- SUDS API running (locally or remote)
- `X-API-Key` configured

## Install (editable)
From repo root:

```bash
pip install -e packages/suds-ui