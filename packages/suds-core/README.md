# suds-core

Core library for Sofia Urban Data Service (SUDS).

Includes:
- PostGIS models + SQLAlchemy engine
- ingestion utilities for GeoPackage/GeoJSON/Shapefile
- spatial query services (bbox, radius)
- connectors (Gate, Overpass, Open-Meteo)